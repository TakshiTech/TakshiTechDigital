service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null || request.origin == 'https://www.webdigitalbazaar.com' || request.origin == 'http://localhost:3000';
    }
  }
}
